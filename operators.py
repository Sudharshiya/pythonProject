# basic operators used for calculation

a = 9
b = 4
add = a + b  #adding two values
sub = a - b   #subtracting two values
mul = a * b    # multiplying two values
mod = a % b     #finding the remainder of divifing two values
print(add)     #display the output using print function
print(sub)
print(mul)
print(mod)
